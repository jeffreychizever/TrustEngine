# TrustEngine
Agentic AI tools have some limitations with their built in trust models. This package provides more granular trust policies for tool execution and moves the cognitive load of configuration to the model itself.
